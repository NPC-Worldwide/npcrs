# Configuration file for ReadTheDocs builds
# Modeled after npcpy setup

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  commands:
    - pip install mkdocs mkdocs-material
    - mkdocs build --site-dir _readthedocs/html

mkdocs:
  configuration: mkdocs.yml

formats:
  - htmlzip
